import socket

class InteractionClient:
    def __init__(self, address, port, userid):
        self.address = address
        self.port = port
        self.userid = userid

    def connect():
        
